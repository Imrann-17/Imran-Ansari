#include <stdio.h>

int main(){

    int max,size,i,x;
    int count;
    int fp=-1; //first position
    arr [n]
    ptintf("Enter the size of array: ");
    scanf("%d",&size);

    printf("Enter array elements:");
    for (i=0;i<n;i++){
        scanf("%d",&a[i]);
    }
}

printf("Enter number to search");
scanf("%d",&x);

for (i=0; i< size; i++){
    if (a=[i]==x) {
        count++;

        if (fp==-1){
            fp=i;
        
        }

    }
}

if (count>0){
    printf("Number found \n",x,count);
    printf("first position=%d\n");

}
else{
    printf("Number not founf\n");

}
 return0;
